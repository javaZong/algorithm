图
图的表示，
领接表
邻接矩阵

图的搜索

广度搜索（BFS）
队列+HashSet


图的宽度优先遍历（BFS）
1，利用队列实现
2，从源节点开始依次按照宽度进队列，然后弹出
3，每弹出一个点，把该节点所有没有进过队列的邻接点放入队列
4，直到队列变空
深度度优先遍历(DFS)
1，利用栈实现
2，从源节点开始把节点按照深度放入栈，然后弹出
3，每弹出一个点，把该节点下一个没有进过栈的邻接点放入栈(要先把当前节点入栈)
4，直到栈变空
